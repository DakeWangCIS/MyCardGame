const CardStrategy = require('./CardStrategy');

class TaoStrategy extends CardStrategy {
    execute(gameContext, target) {
        // TODO: implement this, logic of using tao
    }
}

module.exports = TaoStrategy;